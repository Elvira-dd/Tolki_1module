<h1>Лента для вас</h1>
<div class="recThemes">
    <% @themes.each do |theme| %>
    <h2><%= link_to theme.name, theme_path(theme) %></h2>
    <% end %>
</div>
<div class="recPodcasts">
    <% @podcasts_top.each do |podcast| %>
    <%= render partial: "podcastcard", locals: { podcast: podcast } %>
    <% end %>
</div>